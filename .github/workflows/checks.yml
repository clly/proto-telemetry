name: "Go Checks"
on:
    pull_request:
    push:
        branches:
            # It's important that the action also runs on merge to main
            - main

jobs:
    go-checks:
        runs-on: ubuntu-latest
        steps:
        - uses: clly/.github/workflows/go/checks.yml@main